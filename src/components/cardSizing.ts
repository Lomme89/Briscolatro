/**
 * One box for every card-shaped thing inside a picker.
 *
 * A booster shows Napoletane, carte UNO and personaggi side by side, and each
 * of the three components had its own idea of "small": the playing cards came
 * out a third bigger than the rest, so a pack read as three different objects
 * instead of a row of cards. They all take `size="pick"` and land here.
 */
export const PICKER_CARD_BOX = 'w-[62px] sm:w-[74px] h-[86px] sm:h-[102px]';
